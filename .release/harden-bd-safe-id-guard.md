---
bump: minor
---
bd-safe I-BF1 guard: admit native short ids (prefix === local workspace, via `BdExecOptions.localPrefix` / env `PRX_BEADS_PREFIX`) and harden against the foreign refs it claims to refuse — case-insensitive `GH-`/`NOTION-` surface forms, ids hidden after value-less flags, and inline `--flag=<id>` (prx-3vow).
